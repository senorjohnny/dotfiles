[general]
accounts = main
maxsyncaccounts = 1

[Account main]
localrepository = main-local
remoterepository = main-remote
autorefresh = 0.5
quick = 10

[Repository main-local]
type = Maildir
localfolders = ~/.mail

[Repository main-remote]
type = IMAP
remotehost = SECRET_IMAP_SERVER
remoteuser = SECRET_EMAIL
remotepass = SECRET_PASS
cert_fingerprint = SECRET_IMAP_FINGERPRINT
keepalive = 60
holdconnectionopen = yes
ssl = yes

